import { NPM } from 'aurelia-cli/lib/package-managers/npm';

export default function() {
  process.emitWarning('`au run` task is deprecated for the webpack projects, and a potential candidate for removal in future. Consider using `npm start` instead.')

  return (new NPM()).install([], process.cwd(), 'start');
}
